"""Checksum detector (algorithm-based fault tolerance).

Import submodules directly (e.g. ``assay.abft.check``). This package
does not re-export symbols at import time — eager re-exports created a
circular import with ``assay.noise.lookup``.
"""
